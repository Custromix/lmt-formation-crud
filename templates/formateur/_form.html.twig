{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row justify-content-center">
        <button class="btn btn-primary btn-block col-2">Confirmer</button>
    </div>
{{ form_end(form) }}
