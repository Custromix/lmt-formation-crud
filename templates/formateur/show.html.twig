{% extends 'base.html.twig' %}

{% block title %}Formateur{% endblock %}

{% block body %}
    <h1>Formateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ formateur.name }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ formateur.Firstname }}</td>
            </tr>
            <tr>
                <th>Trigram</th>
                <td>{{ formateur.trigram }}</td>
            </tr>
            <tr>
                <th>Skill</th>
                <td>{{ formateur.skill }}</td>
            </tr>
            <tr>
                <th>Salary</th>
                <td>{{ formateur.salary }}</td>
            </tr>
            <tr>
                <th>Urssaf</th>
                <td>{{ formateur.urssaf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('formateur_index') }}">back to list</a>

    <a href="{{ path('formateur_edit', {'id': formateur.id}) }}">edit</a>

    {{ include('formateur/_delete_form.html.twig') }}
{% endblock %}
