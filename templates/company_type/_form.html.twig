{{ form_start(form) }}
    <div class="row justify-content-around">
        {{ (form_row(form.name)) }}
    </div>
    <div style="margin-top: 3%" class="row justify-content-around">
        <div class="col-2">
            <button class="btn btn-primary">{{ button_label|default('Confirmer') }}</button>
        </div>
    </div>
{{ form_end(form) }}
