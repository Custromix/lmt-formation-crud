{% extends 'base.html.twig' %}

{% block title %}StandardTraining{% endblock %}

{% block body %}
    <h1>StandardTraining</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ standard_training.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ standard_training.nom }}</td>
            </tr>
            <tr>
                <th>NbHours</th>
                <td>{{ standard_training.nbHours }}</td>
            </tr>
            <tr>
                <th>TrainingPrice</th>
                <td>{{ standard_training.trainingPrice }}</td>
            </tr>
            <tr>
                <th>Materials</th>
                <td>{{ standard_training.materials }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ standard_training.reference }}</td>
            </tr>
            <tr>
                <th>Family</th>
                <td>{{ standard_training.family }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('standard_training_index') }}">back to list</a>

    <a href="{{ path('standard_training_edit', {'id': standard_training.id}) }}">edit</a>

    {{ include('standard_training/_delete_form.html.twig') }}
{% endblock %}
