<form method="post" action="{{ path('standard_training_delete', {'id': standard_training.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ standard_training.id) }}">
    <button class="btn">Delete</button>
</form>
