{% extends 'base.html.twig' %}

{% block title %}ActionTrainee{% endblock %}

{% block body %}
    <h1>ActionTrainee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ action_trainee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ action_trainee.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('action_trainee_index') }}">back to list</a>

    <a href="{{ path('action_trainee_edit', {'id': action_trainee.id}) }}">edit</a>

    {{ include('action_trainee/_delete_form.html.twig') }}
{% endblock %}
