<form method="post" action="{{ path('action_trainee_delete', {'id': action_trainee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ action_trainee.id) }}">
    <button class="btn">Delete</button>
</form>
